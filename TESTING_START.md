# Тестирование регистрации нового пользователя

## ✅ Что исправлено:
1. **Полное логирование** - каждый шаг регистрации логируется
2. **Обработка ошибок** - try/catch на каждом этапе
3. **Валидация имени** - проверка длины и HTML-экранирование
4. **Информативные сообщения** - пользователь понимает, что не так
5. **Очистка данных** - context.user_data.clear() после регистрации

## 📝 Как протестировать:

### 1. Запуск бота
```bash
cd c:\Users\Тимур\OneDrive\Desktop\tgbotweather
python main.py
```

### 2. Тест успешной регистрации

**Шаги:**
1. Отправьте боту: `/start`
2. Введите имя: `Тимур`
3. Выберите часовой пояс из списка (например, Москва)
4. Введите город: `Бишкек` ИЛИ отправьте GPS
5. **Ожидается:** Сообщение "Настройка завершена!" + прогноз погоды

### 3. Тест обработки ошибок

**Тест 1: Несуществующий город**
1. `/start`
2. Имя: `Тест`
3. Выбрать timezone
4. Город: `АсдфАсдф123`
5. **Ожидается:** "Не удалось найти город..." с подсказками

**Тест 2: Короткое имя**
1. `/start`
2. Имя: `А`
3. **Ожидается:** "Имя слишком короткое"

**Тест 3: Длинное имя**
1. `/start`
2. Имя: `АААааааааааааааааааааааааааааааааааааааааааааааааааа`
3. **Ожидается:** "Имя слишком длинное"

### 4. Проверка логов

После каждого теста проверяйте `bot.log`:
```bash
Get-Content bot.log -Tail 50
```

**Ищите:**
- ✅ `👤 User X запустил /start`
- ✅ `📝 User X ввел имя: Y`
- ✅ `✅ Координаты найдены для Z`
- ✅ `💾 Сохранение данных пользователя`
- ✅ `🎉 Регистрация user X завершена успешно!`

## 🐛 Если что-то не работает:

### Проблема: "Invalid token"
**Решение:** Проверьте `.env` файл:
```bash
cat .env
```
Убедитесь, что `TELEGRAM_BOT_TOKEN` корректный.

### Проблема: "Сервис геолокации недоступен"
**Решение:** Проверьте `WEATHERAPI_KEY` в `.env`

### Проблема: "Ошибка при сохранении в БД"
**Решение:** 
```bash
# Проверьте миграции
alembic current
alembic upgrade head
```

## 📊 Мониторинг

Следите за логами в реальном времени:
```powershell
Get-Content -Path "bot.log" -Wait -Tail 20
```
