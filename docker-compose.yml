version: '3.8'

services:
  bot:
    build: .
    volumes:
      - .:/app
      - weather_data:/app/data
    env_file:
      - .env
    environment:
      - DATABASE_PATH=/app/data/weather_bot.db
    restart: always

volumes:
  weather_data:
