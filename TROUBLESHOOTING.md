# 🔍 Быстрая Диагностика Бота - Шпаргалка

## Если бот не отвечает

### 1. Проверка токенов
```bash
# Проверьте .env файл
cat .env | grep TOKEN
cat .env | grep WEATHERAPI_KEY
```

**Должно быть:**
```
TELEGRAM_BOT_TOKEN=8547970379:AAGcJUD9pmgvWq5pwfe9U7mZiUXmlJCDId8
WEATHERAPI_KEY=ваш_ключ_здесь
```

### 2. Проверка запуска
```bash
python main.py
```

**Ожидаемый вывод:**
```
INFO - 🚀 Bot is up and running!
INFO - Application started
```

---

## Если кнопка не работает

### Шаг 1: Найдите callback_data
Откройте `keyboards.py` и найдите константу кнопки:
```python
CHANGE_TIME = "change_time"
```

### Шаг 2: Проверьте обработчик в main.py
```python
# Должна быть строка:
application.add_handler(CallbackQueryHandler(change_time_handler, pattern=f"^{CHANGE_TIME}$"))
```

### Шаг 3: Проверьте импорт обработчика
```python
from handlers.settings import change_time_handler
```

### Шаг 4: Проверьте сам обработчик
Откройте файл обработчика и убедитесь, что функция существует:
```python
async def change_time_handler(update: Update, context: ContextTypes.DEFAULT_TYPE):
    ...
```

---

## Частые Ошибки и Решения

### ❌ ImportError: cannot import name 'get_timezone_keyboard'
**Причина:** Функция не экспортирована  
**Решение:** Проверьте `keyboards.py`:
```python
from timezones import get_timezone_keyboard, get_extended_timezone_keyboard
```

### ❌ UnicodeEncodeError: surrogates not allowed
**Причина:** Неправильная запись эмодзи в коде  
**Решение:** Замените `\ud83d\udc54` на `👔` или `\U0001F454`

### ❌ ImportError: cannot import name 'get_user_streak'
**Причина:** Неправильное имя функции в `services/weather_service.py`
**Решение:** Исправлено в версии 2.1. Если возникает снова, проверьте `streak.py`.

### ❌ Conflict: terminated by other getUpdates request
**Причина:** Запущено несколько копий бота одновременно.
**Решение:**
1. Остановите все процессы Python:
   ```bash
   # Windows PowerShell
   taskkill /F /IM python.exe
   ```
2. Запустите бота снова:
   ```bash
   python main.py
   ```

### ❌ Кнопка не реагирует
**Причина:** Обработчик не зарегистрирован в `main.py`  
**Решение:** Добавьте в `main.py`:
```python
application.add_handler(CallbackQueryHandler(your_handler, pattern="^your_pattern$"))
```

### ❌ Текстовая кнопка не работает
**Причина:** Неправильная проверка в `handle_text_input`  
**Решение:** Проверьте, что сравнивается текст кнопки, а не callback_data:
```python
if text == "🌤 Погода":  # ✅ Правильно
    return await weather_now_handler(update, context)

# НЕ ТАК:
if text == WEATHER_NOW:  # ❌ Неправильно (это "weather_now")
```

### ❌ Бот "застревает" в диалоге
**Причина:** Состояние не очищается  
**Решение:** Проверьте, что в начале `start()` есть:
```python
context.user_data.clear()
```

И в `main.py`:
```python
ConversationHandler(
    ...
    allow_reentry=True  # ← Это важно!
)
```

---

## Быстрая Проверка Всех Систем

```bash
# 1. Проверка импортов
python check_bot.py

# 2. Полная проверка
python bot_verification_report.py

# 3. Запуск бота
python main.py
```

---

## Карта Файлов

```
tgbotweather/
├── main.py                          ← Точка входа, регистрация обработчиков
├── config.py                        ← Переменные окружения
├── keyboards.py                     ← Все кнопки и клавиатуры
├── timezones.py                     ← Часовые пояса
│
├── handlers/
│   ├── start.py                     ← Регистрация (/start)
│   ├── weather.py                   ← Погода
│   ├── settings.py                  ← Настройки (время, имя, timezone)
│   ├── cities.py                    ← Города (добавить, удалить)
│   ├── stats.py                     ← Статистика
│   ├── menu.py                      ← Главное меню, помощь
│   └── text_input.py                ← Обработка текстовых кнопок
│
├── services/
│   └── weather_service.py           ← Генерация сообщений о погоде
│
├── database/
│   ├── __init__.py                  ← Функции БД
│   ├── models.py                    ← Модели SQLAlchemy
│   └── session.py                   ← Сессия БД
│
└── recommendations.py               ← Советы по одежде
```

---

## Логи и Отладка

### Включить подробные логи
В `config.py`:
```python
LOG_LEVEL = "DEBUG"  # Вместо "INFO"
```

### Просмотр логов в реальном времени
```bash
# Linux/Mac
tail -f bot.log

# Windows PowerShell
Get-Content -Path bot.log -Wait -Tail 20
```

### Поиск ошибок в логах
```bash
# Linux/Mac
grep ERROR bot.log

# Windows PowerShell
Select-String -Path bot.log -Pattern "ERROR"
```

---

## Контакты для Помощи

**Документация:**
- `FINAL_VERIFICATION_REPORT.md` - Полный отчет
- `VERIFICATION_DONE.md` - Инструкция по проверке
- `START_FIXED.md` - История исправлений

**Скрипты проверки:**
- `check_bot.py` - Проверка импортов
- `bot_verification_report.py` - Полная проверка

---

**Последнее обновление:** 2026-02-16
